;
; BIND data file for $DOMAIN_NAME
;
$TTL 86400
$ORIGIN $DOMAIN_NAME.
@       IN SOA localhost. mail.$DOMAIN_NAME. (
	      1	; Serial
	 604800	; Refresh
	  86400	; Retry
	2419200	; Expire
	  86400 	; Negative Cache TTL
	  )
@       IN NS localhost.
@       IN A $IP_ADDRESS

